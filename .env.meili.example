# Meilisearch Master Key — generate once per environment:
#   openssl rand -hex 32
# Store the real value in 1Password ("VOD Meilisearch Master Key").
# `.env.meili` (ohne .example) ist in .gitignore.
MEILI_MASTER_KEY=replace_me_with_openssl_rand_hex_32
